import { Disk } from './../models/config.model';

/**
 * Store a file with FTP
 *
 * @param file: File
 * @param filename: string
 * @param disk: Disk
 * @return Promise<void>
 */
export async function storeFtp(file: File, filename: string, disk: Disk): Promise<void> {
  //
}
